module.exports = {
  channel: 'monstercat',
  defaultVolume: 1,
  enableChat: true,
  enableTracklist: true,

  twitchUsername: '', // Only required if you want to send chat messages yourself
  twitchToken: '', // https://twitchapps.com/tmi | Same as above
  clientId: '' // https://www.twitch.tv/settings/connections | Always required to get the stream
}
